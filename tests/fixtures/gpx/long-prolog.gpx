<?xml version="1.0" encoding="UTF-8"?>
<?display stylesheet="none"?>
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<!-- Synthetic licence and provenance header. XML allows comments, processing instructions and whitespace before the root element, and a document that uses more than four kilobytes of them is still a valid document with a perfectly ordinary root. -->
<gpx version="1.1" creator="SyntheticExporter"
     xmlns="http://www.topografix.com/GPX/1/1">
  <trk>
    <name>Synthetic track behind a long prolog</name>
    <trkseg>
      <trkpt lat="47.0000" lon="8.0000">
        <ele>400.0</ele>
        <time>2026-07-20T06:00:00Z</time>
      </trkpt>
      <trkpt lat="47.0050" lon="8.0050">
        <ele>410.0</ele>
        <time>2026-07-20T06:05:00Z</time>
      </trkpt>
    </trkseg>
  </trk>
</gpx>
